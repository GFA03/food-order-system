# Tests never hit a real LLM or restaurant-service: the ChatModel is mocked and the
# RestClient is unused (or stubbed). These URLs only need to resolve, not be reachable.
spring.ai.ollama.base-url=http://localhost:11434
restaurant-service.base-url=http://localhost:8082
